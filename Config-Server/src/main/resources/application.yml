server:
  port: 8086

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Md-Umair-Akola/Hotel-Management-System-Config-file
          clone-on-start: true

